Apps:
users:
feed:
gamification:
places:
itineraries:
social:


Checklist:
Criar tela de configurações:
Configurações:
    Privacidade:
        Alterar Senha
        Autenticação de dois fatores (futuro)
        Visibilidade de conta (atualmente, deixar o perfil fechado continua permitindo que o usuário veja quantidades de seguidores e seguidos. Se o perfil estiver oculto, essas informações devem aparecer como "--". Apenas usuários que seguem devem ser capazes de ver essas informações, isto é, se os toggles de exibição estiverem ativos)

    Idioma:
        Mudar idioma do aplicativo (futuro)

    Notificações: (toggle)
        "Usuário X enviou uma mensagem" (notificação de mensagem deve ficar em "mensagem" no navbar (substituir mensagem por ícone de mensagem) e na página de mensagem, usuário que enviou mensagem deve estar destacado)

    Exibição: (toggle)
        Exibir badges de outros usuários (desativar não está ocultando as badges em feed, detalhe de itinerario e comentários)


Tela admin
http://localhost:8000/admin/
http://localhost:8000/admin/login/?next=/admin/

# Terminal 1 — React
cd web
npm run dev

# Terminal 2 — tudo do backend de uma vez só
cd backend
docker compose up

Comandos django agora vem precedidos de:
docker compose exec
Ex: docker compose exec backend python manage.py makemigrations

Criando dados de teste:
docker compose exec backend python manage.py seed_demo_data
Ajustando escala:
docker compose exec backend python manage.py seed_demo_data --usuarios 60 --itinerarios 200 --dias 180
Para limpar e recriar do zero:
docker compose exec backend python manage.py seed_demo_data --limpar

# Terminal 3 - para rodar o Storybook
run npm run storybook
CTRL+C to stop.

# Terminal 4 - subir versão (print) do componente para o Chromatic
npx chromatic --project-token=chpt_6d076130ba669b8


