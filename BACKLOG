Apps:
users:
    Verificar validação de username na criação do usuário.

feed:
    algoritmo para detectar o tipo de lugar/hashtag que o usuário mais tem interesse
    sistema de atualização de feed
    recomendação de posts novos (segundo interesse do usuário) a cada atualização (posts em PaginaExplorar seguem o mesmo modelo)



gamification:
    criar níveis de badge
    criar lógica de concessão de badge no services.py
    testar gamificação

places:

itineraries:

social:

APIs:
/api/places/ sem autenticação de usuário, incluir. ({"detail": "Method \"GET\" not allowed."})
/api/places/autocomplete/?q=
/api/social/comments/
/api/itineraries/fotos/
/api/itineraries/itinerarios/
/api/feed/principal/ sem autenticação de usuário, incluir.

# Terminal 0 - React
npm run dev

# Terminal 1 — Django
python manage.py runserver

# Terminal 2 — Celery worker
celery -A config worker --loglevel=info --pool=solo

# Terminal 3 — Celery beat (agendador)
celery -A config beat --loglevel=info